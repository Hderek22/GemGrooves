{
  "name": "gemgrooves-contracts",
  "version": "1.0.0",
  "description": "GemGrooves smart contracts — music NFT marketplace on Base",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost && npm run sync:web",
    "deploy:testnet": "hardhat run scripts/deploy.js --network base-sepolia && npm run sync:web",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network base && npm run sync:web",
    "sync:web": "node scripts/sync-web-config.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
